# Starting configuration only. Run make init ENV=local to create the private .env.
DEMO_ENV="local"
DEMO_PROJECT="radplanes"
DEMO_DEPLOYMENT="learning"
# Azure initialization also writes AZURE_SUBSCRIPTION_ID and AZURE_LOCATION.
# Do not put discovered endpoints, kubeconfigs, or generated manifests here.
# Optional credentials are entered with --prompt-demo-key or --demo-key-from-env.
# Demo keys require 32-512 printable ASCII characters without whitespace.
